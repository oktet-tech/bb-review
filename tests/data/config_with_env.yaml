# Config with environment variable references
reviewboard:
  url: "https://rb.example.com"
  api_token: "${TEST_RB_TOKEN}"
  bot_username: "test-bot"

llm:
  provider: "anthropic"
  model: "claude-sonnet-4-20250514"
  api_key: "${TEST_LLM_KEY}"

repositories: []
